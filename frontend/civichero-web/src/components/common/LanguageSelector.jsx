import { FormControl,InputLabel,MenuItem,Select } from "@mui/material"; import { useTranslation } from "react-i18next"; const languages=[{code:"en",label:"English"},{code:"mr",label:"मराठी"},{code:"hi",label:"हिन्दी"}]; export default function LanguageSelector(){const{t,i18n}=useTranslation();const value=i18n.resolvedLanguage?.split("-")[0]||"en";return <FormControl size="small" sx={{minWidth:125}}><InputLabel id="language-label">{t("language")}</InputLabel><Select labelId="language-label" label={t("language")} value={value} onChange={(e)=>i18n.changeLanguage(e.target.value)}>{languages.map(x=><MenuItem key={x.code} value={x.code} lang={x.code}>{x.label}</MenuItem>)}</Select></FormControl>}
