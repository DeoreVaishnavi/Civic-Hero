import { Avatar, AvatarGroup, Box, Button, Card, CardContent, Chip, Container, LinearProgress, Typography } from "@mui/material";
import { ArrowForward, AutoAwesome, CameraAltOutlined, CheckCircle, GroupsOutlined, LocationOnOutlined, NotificationsActiveOutlined, ShieldOutlined, TrendingUp } from "@mui/icons-material";
import { Link } from "react-router";

const journey = [
  [CameraAltOutlined, "Capture", "Add clear evidence from your camera or gallery."],
  [LocationOnOutlined, "Pin", "Confirm the exact location with GPS or the map."],
  [NotificationsActiveOutlined, "Follow", "Receive transparent assignment and progress updates."],
  [CheckCircle, "Verify", "Confirm the repair, give feedback, or raise a dispute."],
];
const issues = [
  ["Pothole near JM Road", "In progress", 72, "Roads"],
  ["Overflowing waste point", "Assigned", 44, "Sanitation"],
  ["Streetlight outage", "Resolved", 100, "Electrical"],
];

export default function HomePage() {
  return <><section className="hero-shell relative overflow-hidden"><div className="hero-orb hero-orb-one"/><div className="hero-orb hero-orb-two"/><Container maxWidth="xl" className="relative z-10 grid min-h-[720px] items-center gap-14 py-16 lg:grid-cols-[1.08fr_.92fr]"><div className="reveal-up"><Chip icon={<AutoAwesome />} label="One city. Thousands of everyday heroes." color="secondary" className="float-soft" /><Typography component="h1" variant="h1" sx={{ mt: 3, maxWidth: 760 }}>Turn a neighbourhood problem into <span className="text-gradient">visible progress.</span></Typography><Typography variant="h6" color="text.secondary" sx={{ mt: 3, maxWidth: 650, lineHeight: 1.7 }}>Report civic issues in minutes, follow every municipal action, and help your community verify that the work is truly complete.</Typography><div className="mt-8 flex flex-wrap gap-3"><Button component={Link} to="/report-complaint" variant="contained" size="large" endIcon={<ArrowForward />}>Report a complaint</Button><Button component={Link} to="/community" variant="outlined" size="large" startIcon={<GroupsOutlined />}>Explore community</Button><Button component={Link} to="/report-anonymously" size="large" startIcon={<ShieldOutlined />}>Report anonymously</Button></div><div className="mt-9 flex items-center gap-4"><AvatarGroup max={4}>{["MK","AS","RV","NP"].map((initials, index) => <Avatar key={initials} sx={{ bgcolor: index % 2 ? "secondary.main" : "primary.main", color: index % 2 ? "text.primary" : "white" }}>{initials}</Avatar>)}</AvatarGroup><Typography variant="body2" color="text.secondary"><strong className="text-civic-text">Community-powered</strong><br/>Transparent civic participation</Typography></div></div><Box className="hero-dashboard reveal-up-delay"><Card className="hero-card"><CardContent sx={{ p: { xs: 3, md: 4 } }}><div className="flex items-center justify-between"><div><Typography variant="overline" color="primary">LIVE NEIGHBOURHOOD PULSE</Typography><Typography variant="h4">Issues moving forward</Typography></div><Box className="pulse-dot" /></div><div className="mt-6 space-y-3">{issues.map(([title,status,progress,category], index) => <div key={title} className="issue-row" style={{ animationDelay: `${index * 120}ms` }}><div className="flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-blueberry-50 text-blueberry-600"><LocationOnOutlined /></div><div className="min-w-0 grow"><Typography fontWeight={800} noWrap>{title}</Typography><Typography variant="caption" color="text.secondary">{category} · {status}</Typography></div><Typography fontWeight={900} color="primary">{progress}%</Typography></div><LinearProgress variant="determinate" value={progress} sx={{ mt: 1.5, height: 7, borderRadius: 8 }} /></div>)}</div><div className="mt-5 grid grid-cols-3 gap-2 rounded-2xl bg-cream-200 p-4 text-center"><div><Typography variant="h5">24/7</Typography><Typography variant="caption">Tracking</Typography></div><div><Typography variant="h5">3</Typography><Typography variant="caption">Languages</Typography></div><div><Typography variant="h5">100%</Typography><Typography variant="caption">Transparent</Typography></div></div></CardContent></Card></Box></Container></section><section className="bg-white py-20"><Container maxWidth="xl"><div className="mx-auto mb-12 max-w-2xl text-center"><Typography variant="overline" color="primary">FROM REPORT TO RESOLUTION</Typography><Typography variant="h2">A clear path to a better street</Typography><Typography color="text.secondary" sx={{ mt: 2 }}>Every step is designed to be quick for citizens and accountable for municipal teams.</Typography></div><div className="stagger-grid grid gap-5 md:grid-cols-2 xl:grid-cols-4">{journey.map(([Icon,title,description], index) => <Card key={title} className="feature-card"><CardContent sx={{ p: 4 }}><Typography className="step-number">0{index + 1}</Typography><div className="feature-icon"><Icon /></div><Typography variant="h5" sx={{ mt: 3 }}>{title}</Typography><Typography color="text.secondary" sx={{ mt: 1.5, lineHeight: 1.7 }}>{description}</Typography></CardContent></Card>)}</div></Container></section><section className="py-20"><Container maxWidth="lg"><Box className="community-cta grid items-center gap-8 overflow-hidden rounded-[36px] p-8 text-white md:grid-cols-[1fr_auto] md:p-12"><div><Typography variant="h3">Prefer privacy? Your voice still matters.</Typography><Typography sx={{ mt: 2, maxWidth: 650, opacity: .84 }}>Anonymous reports use secure tracking tokens. Municipal teams can address the issue without seeing your identity.</Typography></div><Button component={Link} to="/report-anonymously" variant="contained" color="secondary" size="large">Start private report</Button></Box></Container></section><section className="bg-blueberry-700 py-16 text-white"><Container maxWidth="lg" className="flex flex-col items-center justify-between gap-6 text-center md:flex-row md:text-left"><div><Typography variant="h3">Your city improves when you take the first step.</Typography><Typography sx={{ mt: 1, opacity: .75 }}>Join, report, discuss, verify, and earn civic recognition.</Typography></div><Button component={Link} to="/register" color="secondary" variant="contained" size="large" endIcon={<TrendingUp />}>Become a CivicHero</Button></Container></section></>;
}
