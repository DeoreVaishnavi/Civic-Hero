import { Avatar, Box, Chip, MenuItem, Paper, Table, TableBody, TableCell, TableHead, TableRow, TextField, Typography } from "@mui/material";
import { EmojiEvents, WorkspacePremium } from "@mui/icons-material";
import { useState } from "react";
import { useQuery } from "@tanstack/react-query";
import { rewardApi } from "../../api/rewardApi";
import PageHeader from "../../components/common/PageHeader";
import EmptyState from "../../components/common/EmptyState";
export default function LeaderboardPage(){const[timeframe,setTimeframe]=useState("AllTime");const{data=[]}=useQuery({queryKey:["rewards","leaderboard",timeframe],queryFn:()=>rewardApi.leaderboard({timeframe})});const items=data.items||data;const podium=items.slice(0,3);return <><PageHeader title="Community leaderboard" description="Recognition for verified contributions—not noise or complaint volume." action={<TextField select value={timeframe} onChange={e=>setTimeframe(e.target.value)}>{["Weekly","Monthly","AllTime"].map(value=><MenuItem key={value} value={value}>{value}</MenuItem>)}</TextField>}/>{podium.length>0&&<Box className="community-cta mb-6 grid gap-4 rounded-[28px] p-6 text-white sm:grid-cols-3">{podium.map((person,index)=><div key={person.userId} className="rounded-2xl bg-white/10 p-4 text-center"><Avatar sx={{mx:"auto",width:58,height:58,bgcolor:index===0?"secondary.main":"white",color:"text.primary",fontWeight:900}}>{person.citizenName?.[0]}</Avatar><Typography variant="h6" sx={{mt:1}}>{person.citizenName}</Typography><Typography sx={{opacity:.7}}>#{person.rank} · {person.points} points</Typography>{index===0&&<EmojiEvents color="secondary"/>}</div>)}</Box>}{items.length?<Paper className="overflow-x-auto"><Table><TableHead><TableRow sx={{bgcolor:"#F8F9FC"}}>{["Rank","Citizen","Points","Resolved","Tier"].map(label=><TableCell key={label} sx={{fontWeight:850}}>{label}</TableCell>)}</TableRow></TableHead><TableBody>{items.map(person=><TableRow hover key={person.userId} selected={person.isCurrentUser}><TableCell><Typography fontWeight={900}>#{person.rank}</Typography></TableCell><TableCell><div className="flex items-center gap-2"><Avatar sx={{width:32,height:32,bgcolor:"primary.light",color:"primary.main"}}>{person.citizenName?.[0]}</Avatar>{person.citizenName}</div></TableCell><TableCell>{person.points}</TableCell><TableCell>{person.closedComplaints}</TableCell><TableCell><Chip size="small" icon={<WorkspacePremium/>} label={person.tier}/></TableCell></TableRow>)}</TableBody></Table></Paper>:<EmptyState title="The leaderboard is waiting for its first hero"/>}</>}
