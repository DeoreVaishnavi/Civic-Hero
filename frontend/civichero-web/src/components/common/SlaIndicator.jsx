import PropTypes from "prop-types"; import { Chip } from "@mui/material"; export default function SlaIndicator({dueAt,isOverdue}){if(!dueAt)return <Chip size="small" label="No SLA" variant="outlined"/>;const overdue=isOverdue||new Date(dueAt)<new Date();const hours=Math.ceil(Math.abs(new Date(dueAt)-new Date())/3600000);return <Chip size="small" color={overdue?"error":hours<=24?"warning":"success"} label={overdue?`Overdue ${hours}h`:`Due in ${hours}h`}/>} SlaIndicator.propTypes={dueAt:PropTypes.string,isOverdue:PropTypes.bool};
