import { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import axiosInstance from '../../api/axiosInstance.js';
const unwrap=(r)=>r.data?.data??r.data;
export default function CitizenVerification(){const [items,setItems]=useState([]);const [error,setError]=useState('');useEffect(()=>{axiosInstance.get('/verification/pending').then((r)=>{const d=unwrap(r);setItems(d.items??d??[]);}).catch((e)=>setError(e.message||'Unable to load pending verifications.'));},[]);return <section className="space-y-5 p-6"><div><p className="text-sm font-semibold uppercase tracking-[.18em] text-sky-400">Citizen action</p><h2 className="mt-2 text-3xl font-black text-white">Pending verifications</h2><p className="mt-2 text-slate-400">Review resolution evidence before approving or rejecting completed work.</p></div>{error&&<p className="rounded-xl bg-rose-500/10 p-4 text-rose-200">{error}</p>}<div className="grid gap-4 md:grid-cols-2">{items.length===0?<p className="rounded-2xl border border-white/10 bg-white/5 p-6 text-slate-400">No complaints currently need your verification.</p>:items.map((x)=><article key={x.id??x.complaintId} className="rounded-2xl border border-white/10 bg-white/5 p-5"><h3 className="font-bold text-white">{x.title??`Complaint ${x.complaintId}`}</h3><p className="mt-2 text-sm text-slate-400">{x.status??'Verification pending'}</p><Link to={`/citizen/complaints/${x.complaintId??x.id}`} className="mt-4 inline-block rounded-lg bg-sky-500 px-4 py-2 text-sm font-bold text-white">Open verification</Link></article>)}</div></section>;}
