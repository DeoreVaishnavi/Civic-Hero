import { Component } from "react"; import PropTypes from "prop-types"; import { Alert,Button } from "@mui/material"; export default class ErrorBoundary extends Component{state={error:null};static getDerivedStateFromError(error){return{error}}render(){if(this.state.error)return <div className="m-6"><Alert severity="error" action={<Button onClick={()=>location.reload()}>Reload</Button>}>The page could not be displayed.</Alert></div>;return this.props.children}} ErrorBoundary.propTypes={children:PropTypes.node.isRequired};
