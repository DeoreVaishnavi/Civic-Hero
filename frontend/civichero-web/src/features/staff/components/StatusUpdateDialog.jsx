import PropTypes from "prop-types"; import { useState } from "react"; import { Button,Dialog,DialogActions,DialogContent,DialogTitle,MenuItem,TextField } from "@mui/material"; export default function StatusUpdateDialog({open,onClose,onSubmit}){const[value,setValue]=useState({status:"InProgress",publicRemarks:"",evidence:null});return <Dialog open={open} onClose={onClose}><DialogTitle>Update complaint status</DialogTitle><DialogContent className="space-y-4 pt-2"><TextField fullWidth select label="Status" value={value.status} onChange={e=>setValue({...value,status:e.target.value})}>{["InProgress","AwaitingInformation","ResolutionSubmitted","Resolved","Rejected"].map(x=><MenuItem key={x} value={x}>{x}</MenuItem>)}</TextField><TextField fullWidth multiline minRows={3} label="Public remarks" value={value.publicRemarks} onChange={e=>setValue({...value,publicRemarks:e.target.value})}/><Button component="label" variant="outlined">Upload evidence<input hidden type="file" accept="image/jpeg,image/png" onChange={e=>setValue({...value,evidence:e.target.files[0]})}/></Button></DialogContent><DialogActions><Button onClick={onClose}>Cancel</Button><Button variant="contained" onClick={()=>onSubmit(value)}>Update status</Button></DialogActions></Dialog>} StatusUpdateDialog.propTypes={open:PropTypes.bool.isRequired,onClose:PropTypes.func.isRequired,onSubmit:PropTypes.func.isRequired};
