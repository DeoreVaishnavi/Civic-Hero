import { useEffect,useState } from "react"; import { Alert,Snackbar } from "@mui/material"; export default function ToastHost(){const[state,setState]=useState({open:false,message:"",severity:"success"});useEffect(()=>{const handler=e=>setState({open:true,...e.detail});window.addEventListener("civichero:toast",handler);return()=>window.removeEventListener("civichero:toast",handler)},[]);return <Snackbar open={state.open} autoHideDuration={4500} onClose={()=>setState(x=>({...x,open:false}))}><Alert severity={state.severity} variant="filled">{state.message}</Alert></Snackbar>}
