import { Breadcrumbs as MuiBreadcrumbs,Link,Typography } from "@mui/material"; import { Link as RouterLink,useLocation } from "react-router"; export default function Breadcrumbs(){const parts=useLocation().pathname.split("/").filter(Boolean);return <MuiBreadcrumbs className="mb-4"><Link component={RouterLink} to="/">Home</Link>{parts.map((p,i)=>i===parts.length-1?<Typography key={p}>{p.replaceAll("-"," ")}</Typography>:<Link component={RouterLink} key={p} to={`/${parts.slice(0,i+1).join("/")}`}>{p.replaceAll("-"," ")}</Link>)}</MuiBreadcrumbs>}
