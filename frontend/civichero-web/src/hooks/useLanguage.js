import { useTranslation } from "react-i18next"; export default function useLanguage() { const { i18n } = useTranslation(); return { language: i18n.resolvedLanguage?.split("-")[0] || "en", changeLanguage: i18n.changeLanguage.bind(i18n) }; }
