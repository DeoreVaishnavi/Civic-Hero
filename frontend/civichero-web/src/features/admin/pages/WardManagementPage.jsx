import { useState } from "react"; import { Button } from "@mui/material"; import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query"; import { wardApi } from "../../../api/wardApi"; import PageHeader from "../../../components/common/PageHeader"; import { notify } from "../../../utils/notify"; import EntityEditorDialog from "../components/EntityEditorDialog"; import ManagementTable from "../components/ManagementTable"; const empty={departmentId:0,name:"",code:"",boundaryNorth:0,boundarySouth:0,boundaryEast:0,boundaryWest:0,isActive:true};const fields=[{key:"departmentId",label:"Department ID",type:"number",required:true},{key:"name",label:"Ward name",required:true},{key:"code",label:"Ward code",required:true},{key:"boundaryNorth",label:"North",type:"number"},{key:"boundarySouth",label:"South",type:"number"},{key:"boundaryEast",label:"East",type:"number"},{key:"boundaryWest",label:"West",type:"number"}];export default function WardManagementPage(){const[value,setValue]=useState(empty),[open,setOpen]=useState(false);const qc=useQueryClient();const{data=[]}=useQuery({queryKey:["admin","wards"],queryFn:wardApi.list});const save=useMutation({mutationFn:()=>value.id?wardApi.update(value.id,value):wardApi.create(value),onSuccess:()=>{notify("Ward saved");setOpen(false);qc.invalidateQueries({queryKey:["admin","wards"]})}});return <><PageHeader title="Ward management" description="Maintain department boundaries and ward availability." action={<Button variant="contained" onClick={()=>{setValue(empty);setOpen(true)}}>Add ward</Button>}/><ManagementTable rows={data} columns={[{key:"name",label:"Name"},{key:"code",label:"Code"},{key:"departmentName",label:"Department"},{key:"openComplaintCount",label:"Open complaints"}]} onDeactivate={wardApi.deactivate} onEdit={row=>{setValue(row);setOpen(true)}}/><EntityEditorDialog open={open} title={value.id?"Edit ward":"Add ward"} fields={fields} value={value} onChange={setValue} onClose={()=>setOpen(false)} onSave={()=>save.mutate()}/></>}
