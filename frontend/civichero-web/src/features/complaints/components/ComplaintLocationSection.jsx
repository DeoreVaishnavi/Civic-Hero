import PropTypes from "prop-types"; import { Checkbox,FormControlLabel } from "@mui/material"; import LocationPicker from "../../../components/maps/LocationPicker"; export default function ComplaintLocationSection({value,onChange}){return <div className="space-y-4"><LocationPicker value={value} onChange={(location)=>onChange({...value,...location,locationConfirmed:false})}/><FormControlLabel control={<Checkbox checked={!!value.locationConfirmed} onChange={e=>onChange({...value,locationConfirmed:e.target.checked})}/>} label="I confirm this is the actual complaint location"/></div>} ComplaintLocationSection.propTypes={value:PropTypes.object.isRequired,onChange:PropTypes.func.isRequired};
