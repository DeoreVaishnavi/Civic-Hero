import { useEffect, useState } from 'react';
import axiosInstance from '../../api/axiosInstance.js';
const unwrap=(r)=>r.data?.data??r.data;
export default function VerificationQueue(){const [items,setItems]=useState([]);const [error,setError]=useState('');useEffect(()=>{axiosInstance.get('/verification/supervisor/pending').then((r)=>{const d=unwrap(r);setItems(d.items??d??[]);}).catch((e)=>setError(e.message||'Unable to load verification queue.'));},[]);return <section className="space-y-5 p-6"><div><p className="text-sm font-semibold uppercase tracking-[.18em] text-sky-400">Verification SLA</p><h2 className="mt-2 text-3xl font-black text-white">Verification queue</h2></div>{error&&<p className="rounded-xl bg-rose-500/10 p-4 text-rose-200">{error}</p>}<div className="grid gap-4 md:grid-cols-2">{items.length===0?<p className="rounded-2xl border border-white/10 bg-white/5 p-6 text-slate-400">No pending citizen verifications.</p>:items.map((x)=><article key={x.id??x.complaintId} className="rounded-2xl border border-white/10 bg-white/5 p-5"><h3 className="font-bold text-white">{x.title??x.complaintReference}</h3><p className="mt-2 text-sm text-slate-400">Deadline: {x.deadlineUtc?new Date(x.deadlineUtc).toLocaleString():'—'}</p></article>)}</div></section>;}
