import PropTypes from "prop-types"; import { Typography } from "@mui/material"; import { formatDateTime } from "../../../utils/dateFormatter"; export default function StatusTimeline({items=[]}){return <ol className="relative ml-3 border-l-2 border-blueberry-50">{items.map((x,i)=><li key={x.id||i} className="relative mb-6 ml-6"><span className={`absolute -left-[31px] top-1 h-3 w-3 rounded-full ${i===items.length-1?"bg-blueberry-600":"bg-civic-border"}`}/><Typography fontWeight={700}>{x.status||x.action}</Typography><Typography variant="body2" color="text.secondary">{x.remarks||x.description} · {formatDateTime(x.createdAt)}</Typography></li>)}</ol>} StatusTimeline.propTypes={items:PropTypes.array};
