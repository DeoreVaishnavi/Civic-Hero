services:
  backend:
    healthcheck:
      test:
        - CMD-SHELL
        - curl --fail --silent --show-error http://127.0.0.1:8080/health/live || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
