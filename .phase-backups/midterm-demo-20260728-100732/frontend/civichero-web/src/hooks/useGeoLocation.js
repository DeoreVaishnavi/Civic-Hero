import {useEffect,useState} from "react"; export default function useGeoLocation(){const [state,setState]=useState({loading:true,latitude:null,longitude:null,error:null});useEffect(()=>{if(!navigator.geolocation){setState({loading:false,error:"Geolocation unavailable"});return}navigator.geolocation.getCurrentPosition(p=>setState({loading:false,latitude:p.coords.latitude,longitude:p.coords.longitude,error:null}),e=>setState({loading:false,error:e.message}))},[]);return state}
