import { Paper,Table,TableBody,TableCell,TableHead,TableRow } from "@mui/material"; import { useQuery } from "@tanstack/react-query"; import { analyticsApi } from "../../../api/analyticsApi"; import PageHeader from "../../../components/common/PageHeader"; import { formatDateTime } from "../../../utils/dateFormatter"; export default function AuditLogsPage(){const{data={}}=useQuery({queryKey:["admin","audit"],queryFn:()=>analyticsApi.auditLogs({page:1,pageSize:50})});return <><PageHeader title="Audit logs" description="Immutable administrative and security activity history."/><Paper><Table><TableHead><TableRow>{["Time","User","Action","Entity","Status"].map(x=><TableCell key={x}>{x}</TableCell>)}</TableRow></TableHead><TableBody>{(data.items||[]).map(x=><TableRow key={x.id}><TableCell>{formatDateTime(x.createdAt)}</TableCell><TableCell>{x.userEmail||"System"}</TableCell><TableCell>{x.action}</TableCell><TableCell>{x.entityName}</TableCell><TableCell>{x.success?"Success":"Failed"}</TableCell></TableRow>)}</TableBody></Table></Paper></>}
