import { z } from "zod"; export const complaintSchema=z.object({images:z.array(z.instanceof(File)).min(1,"Add at least one photo").max(5),category:z.string().min(1),departmentId:z.coerce.number().positive(),wardId:z.coerce.number().positive(),title:z.string().min(5).max(100),description:z.string().min(20).max(1000),address:z.string().min(3).max(300),latitude:z.coerce.number().min(-90).max(90),longitude:z.coerce.number().min(-180).max(180),locationSource:z.enum(["DEVICE","IMAGE_EXIF","MAP_SELECTION","MANUAL_ADDRESS"]),locationConfirmed:z.literal(true,{error:"Confirm the complaint location"}),possibleEmergency:z.boolean().default(false)});
