import { Box, Card, CardContent, Chip, Typography } from "@mui/material";
import { LocationOn, MapOutlined } from "@mui/icons-material";
import { useQuery } from "@tanstack/react-query";
import { complaintApi } from "../../../api/complaintApi";
import ComplaintMarkerMap from "../../../components/maps/ComplaintMarkerMap";
import PageHeader from "../../../components/common/PageHeader";
export default function StaffMapPage(){const{data={}}=useQuery({queryKey:["staff","map"],queryFn:()=>complaintApi.list({pageSize:50})});const items=data.items||data||[];const active=items.filter(item=>!["Resolved","Closed"].includes(item.status)).length;return <><PageHeader title="Operational complaint map" description="Prioritize field work with an authorized location view of active municipal issues."/><div className="mb-4 grid gap-3 sm:grid-cols-3"><Card><CardContent className="flex items-center gap-3"><Box className="feature-icon"><MapOutlined/></Box><div><Typography variant="h5">{items.length}</Typography><Typography color="text.secondary">Visible complaints</Typography></div></CardContent></Card><Card><CardContent className="flex items-center gap-3"><Box className="feature-icon"><LocationOn/></Box><div><Typography variant="h5">{active}</Typography><Typography color="text.secondary">Active locations</Typography></div></CardContent></Card><Card><CardContent><Chip color="success" label="Live operational view"/><Typography variant="body2" color="text.secondary" sx={{mt:1}}>Select a marker for complaint context.</Typography></CardContent></Card></div><Card className="overflow-hidden"><CardContent sx={{p:1}}><ComplaintMarkerMap complaints={items}/></CardContent></Card></>}
