import PropTypes from "prop-types"; import { Alert, Button } from "@mui/material"; export default function ErrorAlert({error,onRetry}){return <Alert severity="error" action={onRetry?<Button color="inherit" onClick={onRetry}>Retry</Button>:null}>{error?.response?.data?.message||error?.message||"Something went wrong."}</Alert>} ErrorAlert.propTypes={error:PropTypes.object,onRetry:PropTypes.func};
