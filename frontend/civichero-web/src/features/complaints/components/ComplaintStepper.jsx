import PropTypes from "prop-types"; import { Step,StepLabel,Stepper } from "@mui/material"; const steps=["Photo","Details","Location","Review"];export default function ComplaintStepper({activeStep}){return <Stepper activeStep={activeStep} alternativeLabel>{steps.map(x=><Step key={x}><StepLabel>{x}</StepLabel></Step>)}</Stepper>} ComplaintStepper.propTypes={activeStep:PropTypes.number.isRequired};
