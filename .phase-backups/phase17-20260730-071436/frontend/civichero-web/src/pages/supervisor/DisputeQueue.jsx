import {useEffect,useState} from 'react';import {disputeApi} from '../../services/disputeApi.js';
export default function DisputeQueue(){const [items,setItems]=useState([]),[remarks,setRemarks]=useState('Reviewed by supervisor.'),[message,setMessage]=useState('');const load=()=>disputeApi.queue(false).then(setItems);useEffect(()=>{load()},[]);const decide=async(id,decision)=>{await disputeApi.supervisorDecision(id,{decision,remarks});setMessage('Decision saved.');load()};return <section className="p-6 lg:p-10"><h1 className="text-3xl font-black text-white">Dispute review</h1>{message&&<p className="mt-4 text-emerald-300">{message}</p>}<textarea className="input mt-5 min-h-24" value={remarks} onChange={e=>setRemarks(e.target.value)}/><div className="mt-6 space-y-4">{items.map(x=><article key={x.id} className="rounded-2xl border border-white/10 bg-white/[.04] p-5"><h2 className="text-xl font-black text-white">{x.title}</h2><p className="mt-3 text-slate-300">{x.citizenRemarks}</p><div className="mt-4 flex gap-3"><button onClick={()=>decide(x.id,'Rework')} className="rounded-xl bg-amber-500 px-4 py-2 font-black text-slate-950">Order rework</button><button onClick={()=>decide(x.id,'Close')} className="rounded-xl bg-emerald-500 px-4 py-2 font-black text-white">Close case</button><button onClick={()=>decide(x.id,'Uphold')} className="rounded-xl border border-white/15 px-4 py-2 font-bold text-white">Uphold resolution</button></div></article>)}</div></section>}
