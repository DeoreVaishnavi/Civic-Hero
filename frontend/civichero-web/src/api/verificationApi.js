import apiClient,{unwrap} from "./apiClient"; export const verificationApi={pending:async()=>unwrap(await apiClient.get("/verification/pending")),get:async(id)=>unwrap(await apiClient.get(`/verification/${id}`)),geoCheck:async(id,value)=>unwrap(await apiClient.post(`/verification/${id}/geo-check`,value)),decide:async(id,value)=>unwrap(await apiClient.post(`/verification/${id}/decision`,value))};
