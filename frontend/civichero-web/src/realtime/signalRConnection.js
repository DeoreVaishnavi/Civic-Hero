import { HubConnectionBuilder,LogLevel } from "@microsoft/signalr"; import { tokenStore } from "../api/apiClient"; const apiBase=import.meta.env.VITE_API_BASE_URL||"http://localhost:5000/api/v1"; const hubUrl=import.meta.env.VITE_SIGNALR_URL||`${apiBase.replace(/\/api\/v1\/?$/,"")}/hubs/notifications`; let connection; export function getSignalRConnection(){if(!connection)connection=new HubConnectionBuilder().withUrl(hubUrl,{accessTokenFactory:()=>tokenStore.get()||""}).withAutomaticReconnect([0,2000,5000,10000,30000]).configureLogging(LogLevel.Warning).build();return connection}
