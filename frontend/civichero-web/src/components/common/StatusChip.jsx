import PropTypes from "prop-types"; import { Chip } from "@mui/material"; import { useTranslation } from "react-i18next"; import { normalizeCode,STATUS_COLORS } from "../../utils/statusHelpers"; export default function StatusChip({status}){const{t}=useTranslation("complaints");const code=normalizeCode(status);const colors=STATUS_COLORS[code]||STATUS_COLORS.AWAITING_INFORMATION;return <Chip size="small" label={t(`complaintStatus.${code}`,{defaultValue:status})} sx={{backgroundColor:colors.bg,color:colors.color,fontWeight:700}}/>} StatusChip.propTypes={status:PropTypes.string.isRequired};
