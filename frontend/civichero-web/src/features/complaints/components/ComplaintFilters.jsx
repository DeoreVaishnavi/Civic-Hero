import PropTypes from "prop-types";
import { FilterAltOutlined, Search } from "@mui/icons-material";
import { InputAdornment, MenuItem, TextField } from "@mui/material";
export default function ComplaintFilters({value,onChange}){const set=(key,next)=>onChange({...value,[key]:next,page:1});return <div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-5"><TextField label="Search" value={value.search||""} onChange={event=>set("search",event.target.value)} InputProps={{startAdornment:<InputAdornment position="start"><Search fontSize="small"/></InputAdornment>}}/><TextField select label="Status" value={value.status||""} onChange={event=>set("status",event.target.value)}><MenuItem value="">All statuses</MenuItem>{["Submitted","UnderReview","Assigned","InProgress","Resolved","Rejected","Reopened"].map(item=><MenuItem key={item} value={item}>{item.replace(/([a-z])([A-Z])/g,"$1 $2")}</MenuItem>)}</TextField><TextField select label="Priority" value={value.priority||""} onChange={event=>set("priority",event.target.value)}><MenuItem value="">All priorities</MenuItem>{["Low","Medium","High","Critical"].map(item=><MenuItem key={item} value={item}>{item}</MenuItem>)}</TextField><TextField select label="Sort by" value={value.sortBy||"createdAt"} onChange={event=>set("sortBy",event.target.value)}>{[["createdAt","Created date"],["updatedAt","Updated date"],["priority","Priority"],["status","Status"]].map(([key,label])=><MenuItem key={key} value={key}>{label}</MenuItem>)}</TextField><TextField select label="Order" value={value.sortOrder||"desc"} onChange={event=>set("sortOrder",event.target.value)} InputProps={{startAdornment:<InputAdornment position="start"><FilterAltOutlined fontSize="small"/></InputAdornment>}}><MenuItem value="desc">Newest / highest</MenuItem><MenuItem value="asc">Oldest / lowest</MenuItem></TextField></div>}
ComplaintFilters.propTypes={value:PropTypes.object.isRequired,onChange:PropTypes.func.isRequired};
