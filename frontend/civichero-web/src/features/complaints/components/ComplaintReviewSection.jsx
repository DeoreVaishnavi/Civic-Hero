import PropTypes from "prop-types"; import { Alert,Divider,Typography } from "@mui/material"; export default function ComplaintReviewSection({value}){return <div className="space-y-3"><Alert severity="info">Review the details carefully. Your confirmed coordinates will be sent to the municipality.</Alert><Typography variant="h5">{value.title}</Typography><Typography>{value.description}</Typography><Divider/><dl className="grid grid-cols-2 gap-3"><dt>Category</dt><dd>{value.category}</dd><dt>Address</dt><dd>{value.address}</dd><dt>Location source</dt><dd>{value.locationSource}</dd><dt>Coordinates</dt><dd>{Number(value.latitude).toFixed(6)}, {Number(value.longitude).toFixed(6)}</dd><dt>Photos</dt><dd>{value.images.length}</dd></dl></div>} ComplaintReviewSection.propTypes={value:PropTypes.object.isRequired};
