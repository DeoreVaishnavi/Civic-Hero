import { Badge,IconButton } from "@mui/material"; import { NotificationsOutlined } from "@mui/icons-material"; import { useQuery } from "@tanstack/react-query"; import { useNavigate } from "react-router"; import { notificationApi } from "../../api/notificationApi"; export default function NotificationBell(){const nav=useNavigate();const{data}=useQuery({queryKey:["notifications","unread"],queryFn:notificationApi.unread,refetchInterval:60000});const count=data?.count??data?.length??0;return <IconButton color="inherit" aria-label={`${count} unread notifications`} onClick={()=>nav("/notifications")}><Badge badgeContent={count} color="secondary"><NotificationsOutlined/></Badge></IconButton>}
