import PropTypes from "prop-types"; import { Pagination } from "@mui/material"; export default function PaginationControl({page,count,onChange}){return <div className="flex justify-center py-4"><Pagination page={page} count={Math.max(1,count)} onChange={(_,v)=>onChange(v)} color="primary"/></div>} PaginationControl.propTypes={page:PropTypes.number.isRequired,count:PropTypes.number.isRequired,onChange:PropTypes.func.isRequired};
