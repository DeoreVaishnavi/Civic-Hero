import PropTypes from "prop-types"; import { useState } from "react"; import { Button,Dialog,DialogActions,DialogContent,DialogTitle,MenuItem,TextField } from "@mui/material"; export default function AssignmentDialog({open,onClose,onSubmit,departments=[],staff=[]}){const[value,setValue]=useState({departmentId:"",officerId:"",priority:"Medium",internalNote:""});const set=(k,v)=>setValue(x=>({...x,[k]:v}));return <Dialog open={open} onClose={onClose}><DialogTitle>Assign complaint</DialogTitle><DialogContent className="space-y-4 pt-2"><TextField fullWidth select label="Department" value={value.departmentId} onChange={e=>set("departmentId",e.target.value)}>{departments.map(x=><MenuItem key={x.id} value={x.id}>{x.name}</MenuItem>)}</TextField><TextField fullWidth select label="Staff member" value={value.officerId} onChange={e=>set("officerId",e.target.value)}>{staff.map(x=><MenuItem key={x.id} value={x.id}>{x.fullName}</MenuItem>)}</TextField><TextField fullWidth select label="Priority" value={value.priority} onChange={e=>set("priority",e.target.value)}>{["Low","Medium","High","Critical"].map(x=><MenuItem key={x} value={x}>{x}</MenuItem>)}</TextField><TextField fullWidth multiline label="Internal note" value={value.internalNote} onChange={e=>set("internalNote",e.target.value)}/></DialogContent><DialogActions><Button onClick={onClose}>Cancel</Button><Button variant="contained" onClick={()=>onSubmit(value)}>Assign</Button></DialogActions></Dialog>} AssignmentDialog.propTypes={open:PropTypes.bool.isRequired,onClose:PropTypes.func.isRequired,onSubmit:PropTypes.func.isRequired,departments:PropTypes.array,staff:PropTypes.array};
