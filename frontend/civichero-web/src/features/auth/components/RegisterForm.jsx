import PropTypes from "prop-types"; import { zodResolver } from "@hookform/resolvers/zod"; import { Alert,Button,TextField } from "@mui/material"; import { useForm } from "react-hook-form"; import { useTranslation } from "react-i18next"; import { registerSchema } from "../validation/authSchemas"; export default function RegisterForm({onSubmit,error}){const{t}=useTranslation("auth");const{register,handleSubmit,formState:{errors,isSubmitting}}=useForm({resolver:zodResolver(registerSchema)});return <form onSubmit={handleSubmit(onSubmit)} className="space-y-4" noValidate>{error&&<Alert severity="error">{error}</Alert>}{[["fullName","text"],["email","email"],["phoneNumber","tel"],["password","password"],["confirmPassword","password"]].map(([name,type])=><TextField key={name} fullWidth type={type} label={t(name)} {...register(name)} error={!!errors[name]} helperText={errors[name]?.message}/>) }<Button fullWidth type="submit" variant="contained" disabled={isSubmitting}>{t("createAccount")}</Button></form>} RegisterForm.propTypes={onSubmit:PropTypes.func.isRequired,error:PropTypes.string};
