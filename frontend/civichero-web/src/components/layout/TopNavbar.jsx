import PropTypes from "prop-types"; import { AppBar,Avatar,IconButton,Toolbar,Tooltip,Typography } from "@mui/material"; import { Menu as MenuIcon,Logout } from "@mui/icons-material"; import { Link } from "react-router"; import useAuth from "../../hooks/useAuth"; import LanguageSelector from "../common/LanguageSelector"; import NotificationBell from "../notifications/NotificationBell"; export default function TopNavbar({onMenu}){const{user,logout}=useAuth();return <AppBar position="fixed" sx={{zIndex:(t)=>t.zIndex.drawer+1}}><Toolbar className="gap-3"><IconButton color="inherit" onClick={onMenu} aria-label="Open navigation"><MenuIcon/></IconButton><Typography component={Link} to="/" variant="h6" color="inherit" className="grow no-underline">CivicHero</Typography><div className="hidden rounded-lg bg-white p-1 sm:block"><LanguageSelector/></div><NotificationBell/><Avatar sx={{width:34,height:34,bgcolor:"secondary.main",color:"secondary.contrastText"}}>{user?.fullName?.[0]||"U"}</Avatar><Tooltip title="Logout"><IconButton color="inherit" onClick={logout}><Logout/></IconButton></Tooltip></Toolbar></AppBar>} TopNavbar.propTypes={onMenu:PropTypes.func.isRequired};
